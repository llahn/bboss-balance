##############################
# 启用security验证
##############################
security:
  basic:
    enabled: true
  user:
    name: llahn
    password: llahn
spring: 
  application:
    name: eureka-server
  cloud:
    config:
      uri:  http://localhost:8888